{
  "name": "turtleg",
  "private": true,
  "workspaces": [
    "Backend",
    "Frontend"
  ],
  "scripts": {
    "build": "cd Frontend && npm ci --include=dev --silent && npm run build",
    "start": "node Backend/src/index.js",
    "dev": "npm run dev --workspace=Backend",
    "postinstall": "npm install --workspace=Frontend --include=dev"
  }
}
