{
  "name": "turtleg",
  "private": true,
  "workspaces": [
    "Backend",
    "Frontend"
  ],
  "scripts": {
    "build": "cd Frontend && npm install && npm run build",
    "start": "cd Backend && npm install && node src/index.js"
  }
}
